import { BgRedDirective } from './bg-red.directive';

describe('BgRedDirective', () => {
  it('should create an instance', () => {
    const directive = new BgRedDirective();
    expect(directive).toBeTruthy();
  });
});
